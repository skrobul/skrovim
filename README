# Installation
```
git clone git@github.com:skrobul/skrovim.git
ln -s skrovim/ .vim
ln -s skrovim/vimrc .vimrc
git submodule add -f http://github.com/gmarik/vundle.git ~/.vim/bundle/vundle
```

Done!

